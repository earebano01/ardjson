py -3 -m venv laboratoire-1
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process
.\laboratoire-1\scripts\activate
pip install django
pip freeze > requirements.txt

django-admin startproject ardjson
cd ardjson
python manage.py runserver
python manage.py migrate

ajouter la dépendance à DRF dans requirements.txt
djangorestframework==3.14.0

cd ..
pip install -r requirements.txt

déclarons DRF dans la liste des applications
INSTALLED_APPS = [
'rest_framework',
]

create superuser :
python manage.py createsuperuser

create models.py

register models in admin.py

add data using admin

create serializers.py

create views.py for APIView

modify urls.py to show the result

configure router in the urls.py

transform APIView into ModelViewset

update ModelViewset to ReadOnlyModelViewset

apply a filter to returned data 

use a filter transmitted in the urls

added the description field on the order api endpoint and edited serializers.py


